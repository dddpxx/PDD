# PDD 项目保存与同步规则

用户于 2026-09-13 指定唯一正式项目目录：`E:\APP\VSCODE-Project\PDD-project`。
后续代码、文档、提示词、工作流、图片、测试及优化成果均先保存到该目录；其他任务目录仅可临时使用，完成后必须比较并归档回来。

- 开始工作先读 `DM.md`、`CLAUDE.md`、`OPTIMIZATION_LOG.md` 及 `docs/SYNC_POLICY.md`。
- GitHub：`https://github.com/dddpxx/PDD.git`，同步分支 `main`。每天北京时间 04:00 由 Codex 自动化任务同步。
- 用户已授权项目资料的正常提交、推送及远端更新安全归入本地；不强推，不重置或覆盖用户改动。分叉、冲突、并发编辑时停止受影响步骤并报告。
- 每次优化在根目录 `OPTIMIZATION_LOG.md` 记录日期、原因、变化、文件、验证、待办、来源和回退线索。建议、已实现和已验收必须明确区分。
- 凭据、登录态、Cookie、私人订单资料仅留本地，禁止提交。依赖、缓存及模型权重不纳入 Git；有价值的业务产物核对后归档，见同步规则。
- 同步授权不等于运行真实发布、采购、客服发送或 GPU 生图任务的授权。
- 最新六视图方案仍未验收；2026-09-09 模型推荐仅是候选，不能根据旧文档声称生产链路已恢复。
