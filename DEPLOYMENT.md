# 生产部署方案

## 目标边界

首个生产版本采用单队列、单自动化执行器。商品上架仍停在“保存草稿”，由人工审核后点击“提交并上架”；验证码、登录失效、利润守卫拦截和缺少 SKU 映射一律转人工，不自动绕过。

## 拓扑

1. **业务服务**：运行 Module 01~06、08、09、10 的 Python 进程。订单适配器把渠道原始订单转换成 `order_center.parse_order()` 的标准字典；客服适配器调用 `customer_service.py`。
2. **浏览器执行器**：独立 Windows 主机或保留交互桌面的 VM，运行有头 Chromium，挂载加密的商家/买家登录态。Module 07 与后续采购页面操作只在这里运行；不要切换到 headless。
3. **GPU 节点**：ComfyUI 独立运行，业务服务通过内网 `COMFY_BASE_URL` 调用。
4. **持久化**：生产接入前用 PostgreSQL 保存订单、SKU/商品映射、采购任务和 FAQ；对象存储保存生成图片。Redis/Celery 仅在出现并发任务或重试需求后引入。

## 配置与安全

- 从 `backend/.env.example` 注入环境变量，密钥与 cookie 只放密钥管理服务或加密卷，不进镜像、Git 或日志。
- 客户姓名、电话、地址只在订单与采购任务中流转；日志仅记录订单号和状态，不输出收件信息。
- 浏览器执行器使用专用低权限系统账户；登录态目录仅该账户可读。
- 所有外部写操作记录订单号、操作者、时间、结果；真实付款和商品上架保留人工闸门。

## 发布顺序

1. 在测试店铺跑 `python -m pytest -q`，再用一件草稿商品验证 Module 07 的类目、属性、规格、价格库存、服务承诺和保存草稿。
2. 用脱敏订单样本验证 Module 08 只接收已支付订单，且缺 SKU 映射时阻断。
3. 用含源链接、店名、手机号/微信号的样本验证 Module 10 不泄漏供应商信息。
4. 灰度接入一个店铺、一个浏览器执行器；连续 7 天无错单、无隐私泄漏、无未告警登录失效后再扩容。

## 回滚与告警

- 任一平台页面结构变化、验证码、登录失效或连续 3 次任务失败：暂停对应执行器并告警，任务保持待人工状态。
- 错价、毛利不足、每日采购额超限：由现有 Profit Guard 阻断，不允许自动重试绕过。
- 回滚只需停止新任务入队并切回人工发布/采购；已生成内容和订单记录不删除。

## 上线前尚需完成

- 对接真实销售渠道订单与客服消息适配器；当前仓库只提供平台无关的领域逻辑，未猜测私有接口。
- 为订单、映射、任务和 FAQ 增加数据库持久化与幂等键。
- 用真实商家登录态完成 Module 07 选择器验收并固化一份脱敏 DOM fixture。
- 明确客户数据跨平台处理的隐私与合规要求后再开放真实订单流量。
