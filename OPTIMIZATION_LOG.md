# PDD 优化与同步记录

## 2026-09-13｜统一 E 盘保存、补齐云端更新、建立每日同步

- 原因：用户要求最近优化归入 E 盘主目录，并每日北京时间 04:00 同步 GitHub。
- 正式目录：`E:\APP\VSCODE-Project\PDD-project`。仓库：`dddpxx/PDD`，分支 `main`。
- 远端更新：本地原 HEAD `08a7b78`，通过快进更新到 `7fcb879`（包含 `4a6d28d` 上手文档、`39ad912` 发布/订单/客服模块及合并提交）。共15个文件更新，无冲突，本地未提交生图修复保留。
- 本地已实现修复：ComfyUI 地址/代理、素材定位、缺母模和视角保护、缓存失效、模型预检查、错误详情；涉及 `backend/pdd_agent/comfy_client.py`、`config.py`、`creative.py`、`personas.py`、提示词和两个测试文件。详见9月9日报告。
- 新增资料：`AGENTS.md` 固定后续保存位置；`docs/SYNC_POLICY.md` 记录同步规则；根 `.gitignore` 补充凭据和临时目录排除；`docs/optimization-archive/recent-decisions-20260913.md` 归档最近讨论。
- 输出归档：复制 46 个 PDD 历史输出文件（图片、预览、文案清单）到 `docs/optimization-archive/output-snapshot-2026-09-13/`，逐文件验证复制前后及来源 SHA-256 一致，清单见 `manifest-2026-09-13.json`。原文件保留。
- 旧人物资产：工作区原已有11个删除，纳入当前版本变化，旧文件仍保留在 Git 历史。本次未执行资产清理。
- 实际验证：初次捆绑 Python 缺少 requests，复用旧环境兼容包后18项 unittest通过；为新拉取模块在任务临时目录安装 pytest 后，合计24项测试全部通过（`tests` + 两个生图测试文件）。未执行真实发布、采购、客服发送或GPU生成。源码/文档/候选输出的凭据模式扫描未发现命中；忽略的凭据不入库。
- 自动化：Codex `pdd` 已启用，每天北京时间04:00；无变化保持安静，同步完成、失败或需处理时通知。
- 待办：六视图及商品编辑尚未通过验收，FLUX.2 klein 4B仍是候选，未安装/运行。旧虚拟环境引用迁移前路径，业务环境需另行修复/验证。模型权重、依赖、缓存、登录凭据、浏览器调试状态及跨项目输出未上传。
- 同步状态：本记录随本次归档一起提交，实际推送结果及远端 SHA 以本任务核验结果为准；失败保留本地提交供下次重试。
- 回退线索：同步前远端基线 `7fcb879`、本地原基线 `08a7b78`；使用 Git 历史查看/提取所需旧版本，不以 reset 覆盖工作区。
